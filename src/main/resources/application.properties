spring.application.name=medisolution
# Datos de conexión
spring.datasource.url=jdbc:mysql://localhost:3306/medisolution_db
spring.datasource.username=root
spring.datasource.password=sqlroot

#Update crea tablas si no existen o las modifica si se agregan campos
spring.jpa.hibernate.ddl-auto=update

# Para ver en consola lo que spring hace con sql
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true